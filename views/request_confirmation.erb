<!DOCTYPE html>
<html>
  <head>
    <title>MakersBnB</title>
    <link rel="stylesheet" type="text/css" href="style.css">
  </head>
    <header><p style="text-align:right;">Logged in as <%="#{@signed_in_user.user_name}" %>&nbsp;
    <a href="/explore_rooms">Explore Rooms</a>&nbsp;
    <a href="/requests">Requests</a>&nbsp;
    <a href="/logout">Log Out</a>
    <p></header>
  <body>
    <img src='images/makers_logo.jpg' id='logo'>
    <section id='box'>
      <form action="/explore_rooms" method="GET">
        <br><h2>Request made for <%=Room.find_by(id: "#{@current_request.room_id}").title %> </h2><br><hr>
        <img src="<%= Room.find_by(id: "#{@current_request.room_id}").picture_url %>" id='room_url'><hr>
        <h3>From <%= PrintDate.selected_date(@current_request.date_from) %> to 
            <%= PrintDate.selected_date(@current_request.date_to) %></h3><br>
        <h3>Please note that this is not an official booking confirmation. The home</h3>
        <h3>owner will need to approvethis request you will be notified with updates.</h3>
        <br><button type="submit" name="explore" id='big_button' style>Explore more rooms!</button><br>
      </form>
    </section>
  </body>
</html>