<!DOCTYPE html>
<html>
  <head>
    <title>MakersBnB</title>
    <link rel="stylesheet" type="text/css" href="style.css">
  </head>
    <header><p style="text-align:right;">Logged in as <%="#{@signed_in_user.user_name}" %>&nbsp;
    <a href="/explore_rooms">Explore Rooms</a>&nbsp;
    <a href="/requests">Requests</a>&nbsp;
    <a href="/logout">Log Out</a>
    <p></header>
  <body>
    <img src='images/makers_logo.jpg' id='logo'>
    <section id='box'>
      <form action="/room" method="GET">
        <br><hr><img src="<%= Room.find_by(id: "#{@current_request.room_id}").picture_url %>" id='room_url'><hr>
        <br><h3>Unfortunately your booking request for <%=Room.find_by(id: "#{@current_request.room_id}").title %></h3>
        <h3>From <%= PrintDate.selected_date(@current_request.date_from) %> to 
            <%= PrintDate.selected_date(@current_request.date_to) %> has been automatically</h3>
        <h3>declined, the as selected dates are no longer available.</h3><br>
        <h4>Please select different dates to book <%=Room.find_by(id: "#{@current_request.room_id}").title %></h4><br>
        <button type="submit" name="date_choose" id='big_button' style>Choose other dates</button><br>
      </form>
    </section>
  </body>
</html>